<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer glow -->
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="24dp"/>
            <gradient
                android:type="radial"
                android:gradientRadius="32dp"
                android:startColor="@color/neon_blue_transparent"
                android:endColor="@android:color/transparent"/>
        </shape>
    </item>
    <!-- Main button -->
    <item
        android:left="2dp"
        android:right="2dp"
        android:top="2dp"
        android:bottom="2dp">
        <shape android:shape="rectangle">
            <corners android:radius="22dp"/>
            <solid android:color="@color/background_dark"/>
            <stroke
                android:width="2dp"
                android:color="@color/neon_blue"/>
        </shape>
    </item>
</layer-list>
